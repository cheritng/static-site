{
  "name": "static-site",
  "version": "1.0.0",
  "description": "Simple static site with blog",
  "scripts": {
    "build": "NODE_ENV=production node scripts/build.js",
    "dev": "node scripts/build.js && live-server docs"
  },
  "dependencies": {
    "front-matter": "^4.0.2",
    "fs-extra": "^11.3.0",
    "marked": "^9.1.6"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
